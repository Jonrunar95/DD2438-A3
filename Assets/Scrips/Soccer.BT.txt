tree("Root")
  	tree("version1")

tree("version1")
	fallback
		sequence
			BisGoalie tree("Goalie")
		sequence
			BisChaser tree("Chaser")
		sequence
			BisCenter tree("Center")

tree("Chaser")
    fallback
        sequence
            BisBehindBall
            BDribble
        sequence
            BisInfrontBall
            BGoBehind

tree("Goalie")
    fallback
        sequence
            BDefend 0.15


tree("Center")
    fallback
        sequence
            BDefend 0.35